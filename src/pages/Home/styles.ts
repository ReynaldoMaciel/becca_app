import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  backgroundImage: {
    flex: 1,
    width: '100%',
    height: '100%'
  },
  logo: {
    alignSelf: 'center',
    height: 60,
    marginTop: 70,
    marginBottom: 30,
    width: '100%',
  }
});

export default styles