import React from 'react';
import { ScrollView, Text } from 'react-native';

interface HomeProps {}

const Home = (props: HomeProps) => {

  return (
    <ScrollView>
      <Text>asdf</Text>
    </ScrollView>
  );
};

export default Home;
